<svg xmlns="http://www.w3.org/2000/svg"
     th:attr="viewBox=${'0 0 ' + size + ' ' + size}"
     version="1.1"
     xmlns:th="http://www.thymeleaf.org">
    <circle th:attr="cx=${size / 2},cy=${size / 2},r=${size / 2},fill=${color}" />
</svg>


